[package]
name = "msi-sidecar"
version = "0.1.0"
edition = "2021"
description = "Privileged sidecar for MSI EC register access"

[[bin]]
name = "msi-sidecar"
path = "src/main.rs"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
